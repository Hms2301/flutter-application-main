import 'package:flutter/material.dart';

class userHeart extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Heart',
        style: TextStyle(fontSize: 50),
      ),
    );
  }
}
